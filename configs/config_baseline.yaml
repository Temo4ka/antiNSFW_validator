# @package _global_

# Конфигурация для обучения baseline модели
# Использует baseline_model.yaml вместо model.yaml

defaults:
  - data: data.yaml
  - model: baseline_model.yaml
  - training: training.yaml
  - optimizer: optimizer.yaml
  - logging: logging.yaml
  - trainer: trainer_baseline.yaml
  - _self_

# Общие параметры
seed: 42
